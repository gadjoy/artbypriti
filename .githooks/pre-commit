#!/usr/bin/env bash
# artbypriti-specific gates only.
#
# Secrets, protected branch, determinism, artifacts and commit-message checks are the
# house's job (tools/check_hygiene.py, installed by the house-gates skill). Its hook block
# is prepended above this line and preserves everything below it — so this file must hold
# only what is specific to this repository.
#
# Bypass: git commit --no-verify   (house block also honours HOUSE_GATES_SKIP=1)
set -e
root="$(git rev-parse --show-toplevel)"
python3 "$root/scripts/check-specs.py"
