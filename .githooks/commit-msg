#!/usr/bin/env bash
# artbypriti gates — the diff says what changed; only the message can say why.
# Rejects the absence of meaning, not phrasing. Bypass: git commit --no-verify
set -e
root="$(git rev-parse --show-toplevel)"
"$root/scripts/check-message.sh" "$1"
